error: `use_ref` is called inside a `match` arm; hooks are keyed by call order, so a conditional call shifts every later hook's slot. Hoist it to the top level of `build`. Silence this rule for the whole impl block with `#[rusty::view(allow(conditional_hooks))]`.
  --> tests/ui/hook_in_match_arm.rs:13:29
   |
13 |                 let value = use_ref(ctx, 0i32);
   |                             ^^^^^^^
